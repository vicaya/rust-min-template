{
  "attribution": {
    "commit": "",
    "pr": "",
    "sessionUrl": false
  },
  "permissions": {
    "allow": [
      "Bash(git push --force-with-lease origin claude/*)",
      "Bash(git push --force-with-lease --force-if-includes origin claude/*)",
      "Bash(git push -u --force-with-lease origin claude/*)",
      "Bash(git push -u --force-with-lease --force-if-includes origin claude/*)"
    ]
  }
}
